import React from "react";
import Search from "./Search";

const Definition = () => {
  return (
    <section id="cards" className="text-center">
    <h2 className="mb-5 font-weight-bold">Definitions</h2>
    <Search />
  </section>
  );
};

export default Definition;
